# Stub content for the "credits" easter egg (see main.py's CREDITS_PHRASES
# and SHOW_CREDITS_COMMAND in config.py). The real Cellar ASCII art isn't
# drawn yet — this placeholder just holds the spot so the command, the
# feature flag, and the frontend rendering path can all be wired up and
# play-tested before the actual art exists. Swap CELLAR_ASCII_ART's value
# for the finished piece when it's ready; nothing else needs to change.
CELLAR_ASCII_ART = "[TODO: Cellar ASCII art goes here]"
